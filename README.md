# Fork of [Kamajor's](https://github.com/KamajorQA) Next.js project.

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

This is a Next.js project bootstrapped with `create-next-app`, `Typescript` and `Redux Toolkit`.
